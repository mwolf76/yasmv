MODULE ite;

VAR
	x, y, z : boolean;

INVAR
	x;

INVAR
	! y;

-- z is FALSE
INIT
	z = (x ? y : x);

-- z is TRUE
INIT
	z = (y ? y : x);

-- pick-state should fail here as the above INITs are mutually inconsistent
